var React = require('react');
var D = React.DOM;

var Settings = React.createClass({
    render: function() {
        return D.div(null,
            D.h2(null, 'Settings'));
    }
});

module.exports = Settings;
